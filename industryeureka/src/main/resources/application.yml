server:
  port: 8030
eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
    service-url:
      defaultZone: http://vbooster:123456a@localhost:8030/eureka/
logging:
  config: classpath:logback-spring-test.xml

spring:
  application:
    name: industryeureka
  security:
    user:
      name: vbooster
      password: 123456a