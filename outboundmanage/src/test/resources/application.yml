server:
  port: 8022
  tomcat:
    uri-encoding: UTF-8
  servlet:
    context-path: /om
spring:
  application:
    name: outboundmanage
  jackson:
    serialization:
      indent-output: true
      fail-on-empty-beans: true
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+08
  datasource:
    url: jdbc:mysql://39.97.253.38:3306/smartindustry?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: '!qaZXSw2'
#    url: jdbc:mysql://192.168.10.3:3306/smartindustry?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
#    username: root
#    password: 1234qwer
    driver-class-name: com.mysql.cj.jdbc.Driver
#    schema-username: sa
#    schema-password: sa
    initialization-mode: never
#    platform:
    schema: classpath:schema.sql
#    data: classpath:data.sql
  servlet:
    multipart:
      enabled: true #是否启用http上传处理
      max-request-size: 100MB #最大请求文件的大小
      max-file-size: 100MB #设置单个文件最大长度
mybatis:
  mapper-locations: com/smartindustry/common/mapper/**/*.xml

logging:
  level:
    com:
      smartindustry:
        common:
          mapper: debug
  config: classpath:logback-spring-test.xml
